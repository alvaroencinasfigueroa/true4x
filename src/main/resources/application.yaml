spring:
  application:
    name: true4x

  datasource:
    url: jdbc:postgresql://ep-snowy-night-aim5owds-pooler.c-4.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8083

app:
  jwt:
    secret: ${JWT_SECRET:true4x-super-secret-key-must-be-at-least-32-chars}
    expiration: ${JWT_EXPIRATION:86400000}